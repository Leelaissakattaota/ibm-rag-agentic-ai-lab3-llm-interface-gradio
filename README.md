# IBM Watsonx Chatbot with Gradio Interface

This project demonstrates a simple AI chatbot built using **IBM watsonx.ai** and **Gradio**.

## 🛠️ Tools Used
- **IBM watsonx.ai**: Uses the `Granite-3.3-8b` model for generating responses.
- **Gradio**: Creates the user-friendly web interface.
- **Python**: The core programming language.

## 📂 Files
- `simple_llm.py`: Basic script to test the IBM Watsonx model in the terminal.
- `gradio_demo.py`: A simple example of how Gradio interfaces work.
- `llm_chat.py`: The main chatbot application connecting Watsonx to a web UI.
- `common_input_types.py`: Examples of different Gradio inputs (text, sliders, etc.).

## 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install gradio ibm-watsonx-ai langchain-ibm
